name: praktikum_2_modul
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts : ^6.2.1
  image_picker: ^1.1.2
  camera: ^0.11.1
  flutter_local_notifications: ^19.1.0
  flutter_map: ^8.1.1
  geolocator: ^13.0.2
  flutter_map_location_marker: ^10.0.0
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - images/
